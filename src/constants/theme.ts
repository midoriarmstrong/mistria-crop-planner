import { createTheme } from '@mui/material';

export const theme = createTheme({
  cssVariables: true,
  palette: {
    primary: {
      main: '#0e6878',
      light: '#18adb1',
    },
    secondary: {
      main: '#6ebbe3',
      light: '#d9f0ff',
    },
  },
});
